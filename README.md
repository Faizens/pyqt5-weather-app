# PyQt5 Weather App

A simple weather app I built while learning PyQt5. It uses OpenWeatherMap API to show current weather for any city.

## What it does
- Type a city name, get the weather
- Shows temperature, weather emoji, and description
- Sky blue background because why not

## How to run

1. Install stuff:
```bash
pip install PyQt5 requests
